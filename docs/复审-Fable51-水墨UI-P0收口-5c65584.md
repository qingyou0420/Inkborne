# 墨生万象（Inkborne）· 水墨 UI 方案轮 P0 复审（#136 之后）

> 状态：**复审报告。** 基线 `work/authoring-next` @ `5c65584`（已合 #135 = 方案 `docs/水墨UI方案-Fable51-视觉与信息架构清理-97a2b73.md`，#136 = P0 切片 A/E/C/B/D 施工）。逐项以源码核实「意图是否落地」；方案里的行号 / 片段若已过时，一律以函数 / 组件名与意图判断。
> 本文**不改任何产品代码**，只提交这份报告。不重开创作流程、不实现 P1/P2、不扩安全边界、未跑真模型。

---

## 〇、结论（先读这里）

1. **水墨 UI P0 收口；P1/P2 可另开。** 方案「四·P0」五个切片 A（词表）/ E（圆角与横幅令牌）/ C（重复入口归一）/ B（旧管线入口收口）/ D（本书概览 + 书名可点）在 `packages/studio/src` 全部按意图落地，且每片都有源码文本断言（`ink-copy-glossary.test.ts`、`ink-ui-p0.test.ts`、`copy-dedupe.test.ts`、`ink-home.test.ts` 新增用例 + 6 个既有测试同步改写）。对照表见「一」：A / E / C **通过**，B / D **通过·备注**，无「未通过」。
2. **四阶段书在源码可达的任何页面都碰不到「通过 / 回退 / 组装 / 作品工具」。** 落笔目录 ⋯ 的「通过 / 回退 / 审校」包在 `!authoringBook`；「作品工具」抽屉与「当前作品 ▸」子菜单只在 `authoring?.authoringBook === false` 时渲染（未知态默认隐藏，方向安全）；章页 `ChapterReader` 在 `readOnly` 时整条工具栏（助手 / 编辑 / 通过 / 回退 / 上下文包 / 删除）不渲染，只留「排版」；原始资料页 `canEdit = presentation.canEdit && !pageReadOnly`。聊天快捷动作「落墨 · 写下一章」（`QuickActions`）只在 `mode !== "book"` 出现，问心页不受影响。
3. **双入口已归一。** 统计只剩书架卡片 ⋯（配置 ▸ 工具 ▸ 统计与 `statsBookId` 猜书逻辑一并删除）；导出正文只剩书架卡片 ⋯ + 落笔页 `ExportMenu`（`name="export-format"` 全 `src` 仅 1 处，四阶段 / 旧书分支共用同一组件）；八槽模型只挂 `ServiceListPage`（`StageTools.tsx` 里的另一份无挂载点，属 P2-L 死代码）；织卷 ⋯ 的「生成本次章概要」已删；研墨 ⋯「重新拟定目录」改为复用 `startCatalog`。阶段条书名是 `<button data-testid="book-overview-link" onClick={nav.toBook}>` → `#/book/:id` → `BookStudy`（现在显示「本书」kicker），`study` 态 `aria-current` 落在书名，`ink-home.test.ts` 断言全页恰一个 `aria-current="page"`。
4. **词表在方案覆盖的 9 个文件内基本清净，只剩两条非阻塞残留。** 禁用词 `连载书房 / 创作书房 / 返回书房 / 全书大纲 / 全书规划 / 卷章目录 / 已采用资料 / 真相文件 / 落墨 / 项目设置 / 驾驭舱` 在 9 个文件中零命中。残留：`use-i18n.ts` 的 `settings.modelOverridesHint`（「便宜模型审稿」）与 `nav.deleteAskHint`（「不影响书稿与设定」）各一处；`chapter.readyForReview`「待审稿」与 `BookStudy.statusLabel` 的旧书状态词属方案 U-C-9 明示保留。覆盖文件之外仍有 `activity-copy.ts`「落墨 · …」（运行 ▸ 实时动态 / 守护进程页）、`ShortReader` 的「书房」按钮、`serial-cockpit.ts` 的「请先落墨第 N 章」——都在方案 P1/P2 的范围（P1-J、P2-P、旧书管线文案），列入「三」。
5. **验证：** `pnpm typecheck` 通过（core build + studio client/server tsc）；studio 侧 19 个与 UI 轮相关的测试文件 142 例全绿（`App.test`、`branding-chrome`、`update-chrome`、`works-list-parity`、`p1-ia`、`p2-ask-ground`、`ask-page`、`stage-dot`、`copy-dedupe`、`p0-7-chrome`、`p1-5-deep-pages`、`authoring-ia`、`ink-copy-glossary`、`ink-ui-p0`、`ink-home`、`stage-copy`、`serial-cockpit`、`activity-copy`、`use-hash-route`）。按方案「七·1」口径，这只是合入门槛，**不是视觉验收**；「七·2」的用户实机逐页验收（1480×960 / 1100×720，日夜 × 三种纸色，一本四阶段书 + 一本导入旧书）仍是收口后的最后一道，本文不替代。

---

## 一、P0 切片 A → E → C → B → D 对照表

判定口径：「通过」= 方案该切片的处置意图在代码路径上成立且有代码测试；「通过·备注」= 成立但有不阻塞收口的边角；「未通过」= 意图未成立。

| 切片 | 方案要求（摘要） | 源码核实 | 测试证据 | 判定 |
|---|---|---|---|---|
| **A · 词表替换** | 按 2.3 词表替换 U-C-1…U-C-8；`ServiceListPage` h1 改「配置」；`StudioHeader` aria 改「回到书架」；修 `cockpit.*` en 值；`isZh` 判定改用 `useI18n().lang`（P2-Q 顺手） | `use-i18n.ts`：`settings.title / nav.projectSettings` → 配置，`settings.modelsTab` → 模型，`weave.bookOutline` → 本书规划，`truth.*` → 原始资料，`cockpit.writeNext` → 「写下一章（旧管线）/ Write next (legacy)」，`cockpit.weave` en → Weave，`bread.books / bread.home` → 书架，`book.goWeave` → 去织卷生成章概要。`stage-copy.ts`：`studyGuideCopy / weaveGuideWhenUngrounded / writeEmptyCopy` 按四阶段门禁重写（U-C-8）。`StudioHeader` aria「墨生万象，回到书架」。`ServiceListPage` h1 `t("settings.title")`。`OutlineWorkspace`：卷章目录 → 卷纲目录，目录首项副文案「规划」，全书规划 → 本书规划（5 处提示语），卷纲操作 → 目录操作。`AuthoringWeavePanel` h2「本书规划」、textarea aria「规划候选」、成果操作 → 更多操作。`BookGround` / `AuthoringGroundPanel`：已采用资料 → 已采用设定（旧书），设定任务 → 目录操作。`BookDetail`：当前章节工具 → 目录操作，`write-legacy` summary → 「旧管线」。`AuthoringWritePanel` 成果操作 → 更多操作（U-C-6）。`ShortReader` kicker 创作书房 → 短篇。`BookAskPage` 用 `useI18n().lang`。`DESIGN.md / PRODUCT.md` 死引用改指向方案文档（附录 A 建议） | `ink-copy-glossary.test.ts`：9 文件拼接后不含 9 个禁用词；断言统一标题与 `cockpit.*` en 值。`stage-copy.test.ts`(5) 全绿 | **通过**。残留见「〇·4」：`settings.modelOverridesHint`「审稿」、`nav.deleteAskHint`「书稿」两处未替换，词表测试未覆盖「审稿 / 书稿 / 审校」；`studyGuideCopy` 现已无调用点（`BookStudy` 不再引用），成死导出 |
| **E · 全站圆角与横幅令牌** | `@theme` 把 `--radius-xl` 收到 `var(--radius)`、补 `--radius-2xl`；`rounded-[10px]` → `rounded-lg`；新增 `.ink-notice`，替换 `BookDetail / BookStudy / OutlineWorkspace / ProjectSettings / App` 五处横幅 | `index.css` `@theme inline`：`--radius-xl: var(--radius); --radius-2xl: calc(var(--radius) + 1px)`（`--radius: 5px`，故 xl=5px、2xl=6px，一处全站生效）。`ink-design.css` 新增 `--ink-line / --ink-notice` 与 `.ink-notice`（无底色、左 2px 线、13px、`[data-tone="danger"]` 走 `--destructive`）。全 `src` `rounded-[10px]` 零命中（含 `ui/button.tsx` default/lg、`BookSettingsDrawer`、`ShortSettings`、`ChapterReader`、`BookGround EDITOR_CLASS`、`OutlineWorkspace`、`Sidebar`）。五个文件的 `rounded-2xl` 横幅 → `.ink-notice`：`App` 启动错误卡、`BookDetail` 进行中 / 错误横幅与 `review-queue`（去掉 `bg-mark-soft` 绿底，顺带兑现 U-V-2 一处）、`BookStudy` 错误横幅、`OutlineWorkspace` `outline-ungrounded`、`ProjectSettings` `notice` | `ink-ui-p0.test.ts`「P0-E radius tokens」：令牌断言；遍历 `src` 无 `rounded-[10px]`；五文件无 `rounded-2xl` | **通过**。`ConfirmDialog / BookBusyCard / ChapterWorkspacePanel / ChatPage / TranslationManager / ShortSettings` 仍写 `rounded-2xl`，但经令牌已渲染为 6px，视觉合规；`ProjectSettings` 内 6 处 `rounded-xl … bg-secondary/20` 是卡片不是横幅，属「三·4」设置页 `.ink-section` 降级（P1/P2） |
| **C · 重复入口归一** | U-D-1 删配置菜单「统计」；U-D-2 删配置「导出正文」、落笔两份导出合并为 `ExportMenu`；U-D-4 高级页去第二份 `AuthoringRolesPanel` 并加说明；U-D-6 织卷菜单去重复项；U-D-7 研墨菜单复用 `startCatalog` | `AppMoreMenu.tsx`：删 `dash.stats` 项、`statsBookId`、`bookManuscriptExportPath` 导入与「导出正文」项；`entries` 不再带 `disabled`。新建 `components/ExportMenu.tsx`（单一 `name="export-format"`，`fetchJson /books/:id/export-save`），`BookDetail` 两个分支各渲染一次同组件（互斥分支，DOM 中始终只有一份）。`ProjectSettings.tsx` 删 `AuthoringRolesPanel` 导入与挂载，「全局默认模型」下加「四阶段各槽在模型配置页。」；`ServiceListPage` 保留唯一挂载。`AuthoringWeavePanel` ⋯ 菜单删「生成本次章概要」，按钮「生成本卷章概要」保留。`AuthoringGroundPanel` ⋯「重新拟定目录」→ `void startCatalog()`。书架卡片 ⋯（`Dashboard.tsx`）仍是 作品设置 / 作品统计 / 导出正文 三项，即 U-D-1 / U-D-2 的唯一主入口 ① | `copy-dedupe.test.ts`「keeps one write-page export control and one eight-slot panel」；`authoring-ia.test.ts` 八槽断言改读 `ServiceListPage.tsx`；`p1-ia.test.ts`「P1-2 落笔」改读 `ExportMenu.tsx` | **通过** |
| **B · 旧管线入口收口** | U-L-1 四阶段书「预览」改只读；U-L-2 四阶段书隐藏「作品工具」；U-L-3 真相文件只读化 + 移到高级下 + 改名「原始资料（只读）」；U-L-6 `deriveBookChromeTab` 把 `truth / chapter` 归入 `study` | `AppMoreMenu.tsx`：`useApi("/authoring/workspace?…")` 取一次，`showWorkTools = Boolean(currentBookId) && authoring?.authoringBook === false`；「当前作品 ▸（作品设置 / 作品工具）」与 `<BookToolsDrawer>` 仅在 `showWorkTools`；四阶段书平铺一项「作品设置」（U-D-3 顺带）；「原始资料 / 原始资料（只读）」移到菜单尾部「高级」之后。`ChapterReader.tsx`：`readOnly = authoring?.authoringBook === true` → 状态字「只读预览」(`data-testid="chapter-reader-readonly"`)，`readOnly ? null : <>…</>` 包住助手 / 编辑 / 通过 / ⋯（回退 / 上下文包 / 删除）；正文头「排版」按钮保留（U-D-5，抽屉标题已注「与外观设置同步」）。`TruthFiles.tsx`：`pageReadOnly` → `canEdit` 取反、h1「原始资料（只读）」。`BookDetail` 目录 ⋯：`authoringBook ? "只读预览" : t("reader.preview")`。`App.tsx deriveBookChromeTab`：`truth / chapter → "study"`，`ground / write` 分支不再包含它们，且 `setLastStage` 只对四阶段 tab 生效，故进章页 / 原始资料页不会改写「上次阶段」 | `ink-ui-p0.test.ts`「P0-B authoring chrome close」三条：菜单含 `showWorkTools` 逻辑且不含 `dash.stats / 导出正文`；`ChapterReader / TruthFiles` 有 `readOnly` 分支；`App.tsx` `truth/chapter → study` | **通过·备注**。备注 ①：`ChapterReader.readOnly` 与 `TruthFiles.pageReadOnly` 在 `/authoring/workspace` 未返回前为 `false`，四阶段书的完整工具栏 / 「编辑」会在一次本地往返内闪现（`AppMoreMenu` 用 `=== false` 判定，方向相反、默认隐藏，是安全的）。不是回归（#136 前工具栏始终显示），不一行可修（需在未知态隐藏、错误态回落旧书），列 P1-B'。备注 ②：`BookStudy` 「等你过目 · 第 N 章 待审稿 → 去看」仍指向 `nav.toChapter`，对四阶段书会落到只读章页而非落笔页；触发条件是旧管线 `review-queue` 对末章有 critical 项，四阶段书几乎不会命中，列 P1 |
| **D · 本书概览 + 书名可点** | `BookWorkspaceNav` 书名改 `<button onClick={nav.toBook}>`，`study` 态 `aria-current` 落书名，`data-testid="book-overview-link"`；`BookStudy` 更名「本书」、删旧书「落墨」分支与 `skipPreviousApproval`、「四步一览」去 `StageDot` 改文字状态；`deriveBookChromeTab("book")` 保持 `study`；`ink-home.css` 按钮态 | `BookWorkspaceNav.tsx`：书名 `<button type="button" className="ink-book-context" data-testid="book-overview-link" aria-current={current === "study" ? "page" : undefined} onClick={() => nav.toBook(bookId)}>`；`nav.toBook` 在 `App.tsx` → `setRoute({page:"book"})` → `BookStudy`。`ink-home.css`：`button.ink-book-context{background:none;border:0;padding:0;cursor:pointer}` + `[aria-current="page"]{color:var(--foreground)}`。`BookStudy.tsx`：头部加「本书 / This book」kicker；删除 `cockpit-next-chapter` 整段（含 `study-write-chapter`、`cockpit-write-next-button`、`skipPreviousApproval`、`handleWriteNext`、`startWriteNext` 导入、`studyGuideCopy` 引用、`pageError`、`animate-spin` 圆环）；「本卷已收」并入「等你过目」列表项；`StepLine` 去 `StageDot`，改 `stageStateLabel(state, isZh, isWrite)` 文字列（`grid-cols-[3.5rem_4rem_minmax(0,1fr)]`）；旧书「写下一章」由落笔页 `write-legacy` 的 `write-next-primary`（`t("cockpit.writeNext")` = 「写下一章（旧管线）」）承担，`SerialCockpitStrip` / `review-queue` / 「全部通过」均在 `<details class="write-legacy">` 内 | `ink-home.test.ts`：`active:"weave"` 用例 `aria-current` 恰一次且含 `book-overview-link`；新增 `active:"study"` 用例断言 `aria-current` 落书名、不落任一 `book-step-*`。`authoring-ia.test.ts` / `p1-ia.test.ts`「P1-4 书房」改为断言 `study-four-steps`、无 `study-write-chapter / cockpit-write-next-button / skipPreviousApproval`。`stage-dot.test.ts` / `p2-ask-ground.test.ts` 复跑通过 | **通过·备注**。备注：书名按钮只有 `cursor:pointer`，无 hover / focus-visible 样式，静态下与旧的 `<span>` 长相相同，可发现性偏弱（P1 一行 CSS）；`analytics / truth / chapter` 三个非阶段路由都映射到 `study`，此时书名会带 `aria-current`，语义是「本书范围内」，可接受 |

#136 合入后的第二个提交 `66eba7b`（修 `App.tsx` 错误屏 JSX 闭合、同步测试契约）已包含在基线中，本文以 `5c65584` 树为准。

---

## 二、再扫四项

| 项 | 检查路径 | 结果 |
|---|---|---|
| **四阶段书是否仍能撞到「通过 / 回退 / 组装 / 作品工具」** | `rg` 全 `src`（排除测试与 P2-L 死代码）追 `/approve`、`/reject`、`/compose`、`BookToolsDrawer`、`setBookPanel("tools")`、`toChapter(`、`toTruth`、`QuickActions`：`BookDetail` 目录 ⋯ 的「通过」在 `!authoringBook && ch?.status === "ready-for-review"`，「回退 / 审校 / …」在 `!authoringBook ? <>…</>`；「全部通过」`handleApproveAll` 与 `SerialCockpitStrip` 在 `write-legacy` `<details>` 内；`ChapterReader` `handleApprove / handleReject / handleDelete / handleOpenPacket` 的按钮全部在 `readOnly ? null : <>…</>` 内，`Ctrl+S` 处理器依赖 `editing`，只读态进不了编辑；`ChapterWorkspacePanel`（助手）随工具栏一起隐藏；`/compose` 只在 `BookToolsDrawer`，抽屉只在 `showWorkTools`；`QuickActions`（含「落墨 · 写下一章」）在 `ChatPage` 以 `mode !== "book"` 守卫，问心页不渲染；`TruthFiles` 唯一写路径 `PUT` 在 `canEdit` 后 | **未撞到**（唯一边角是「一 · B 备注 ①」的加载窗口闪现） |
| **统计 / 导出 / 八槽 / 织卷动作双入口** | 统计：`nav.toAnalytics` 调用点 = `Dashboard` 卡片 ⋯ + `BookToolsDrawer.onOpenAnalytics`（仅旧书）；配置菜单无。导出：`bookManuscriptExportPath` 只在 `Dashboard`；`ExportMenu` 只在 `BookDetail`；`name="export-format` 全 `src` 1 处。八槽：`<AuthoringRolesPanel` 挂载 = `ServiceListPage` + `StageTools.tsx`（无导入方，P2-L 待删）。织卷：`openChapterGeneration` 只剩按钮一处调用；研墨 `startCatalog` 两处调用同一函数 | **无明显双入口**。「作品设置」按方案保留书内 / 书外各一（U-D-3） |
| **阶段条书名是否可回「本书」** | `BookWorkspaceNav` 书名 `onClick={() => nav.toBook(bookId)}`；`App.tsx` `nav.toBook → {page:"book"}` → `route.page === "book" && <BookStudy>`；`deriveBookChromeTab("book") === "study"` → 书名 `aria-current="page"`；`BookStudy` 头部「本书」；`TruthFiles` 空态动作 `t("cockpit.title")` = 「打开本书」→ `nav.toBook`。`BookStudy` 本身不再有任何旧管线按钮（`toChapter` 仅用于「等你过目」一项，见 B 备注 ②） | **可回**。另：`ImportManager` 导入成功与 `ChapterReader`（旧书）通过 / 回退 / 删除后仍 `nav.toBook`，落点一致 |
| **词表禁用词在方案覆盖的 9 个文件内是否清掉** | `rg "连载书房\|创作书房\|返回书房\|全书大纲\|全书规划\|已采用资料\|真相文件\|落墨\|项目设置\|驾驭舱\|卷章目录\|书稿\|审稿\|审校\|连续创作与作品工具"` 于 `use-i18n.ts / stage-copy.ts / StudioHeader.tsx / ServiceListPage.tsx / OutlineWorkspace.tsx / AuthoringWeavePanel.tsx / BookGround.tsx / BookStudy.tsx / ShortReader.tsx` | 方案 2.3 表列禁用词零命中。残留 8 处全在 `use-i18n.ts` + `BookStudy.statusLabel`：`translation.*` 三处「审校」是翻译工具的专业词（其他创作，本轮不动）；`book.audit`「审校」、`chapter.readyForReview`「待审稿」、`BookStudy.statusLabel` 「待审稿」是旧书目录状态词（U-C-9 明示不改）；**真正的残留两处**：`settings.modelOverridesHint`「便宜模型审稿」、`nav.deleteAskHint`「不影响书稿与设定」。均是纯字符串，不误导操作，不阻塞 |

---

## 三、不阻塞收口的剩余项（可并行 / 后置）

不给施工顺序以外的时间承诺；每项标出改动落点，便于直接开 PR。**均不是收口条件。**

### 3.1 可与 P1 并行的小项（每项 ≤ 20 行）

| 编号 | 内容 | 落点 |
|---|---|---|
| P1-B' | `ChapterReader.readOnly` / `TruthFiles.pageReadOnly` 在 `/authoring/workspace` 未返回前视为「未知」：工具栏 / 「编辑」在 `authoring === undefined` 时不渲染，`useApi` 报错时回落旧书行为（保持旧书可达）。同时把 `BookStudy` 「等你过目 · 第 N 章」在 `authoringBook` 时改指 `goStage(nav, bookId, "write")` | `pages/ChapterReader.tsx`、`pages/TruthFiles.tsx`、`pages/BookStudy.tsx` |
| P1-D' | `button.ink-book-context:hover, :focus-visible { color: var(--foreground); text-decoration: underline; text-underline-offset: 3px; }`，让书名的「可点」被看见 | `ink-home.css` |
| P1-A' | 词表残留两条：`settings.modelOverridesHint`「审稿」→「审查」；`nav.deleteAskHint`「书稿」→「正文」。`ink-copy-glossary.test.ts` 的禁用词表补 `书稿`、`审稿`（`chapter.readyForReview` 需一并迁出 `use-i18n.ts` 或在测试里显式豁免该键） | `hooks/use-i18n.ts`、`__tests__/ink-copy-glossary.test.ts` |
| P1-A'' | `studyGuideCopy` 已无调用点，删导出与其 `stage-copy.test.ts` 用例；或留待 P2-M 一起清 | `lib/stage-copy.ts` |

### 3.2 方案已排期、本轮不动的 P1 / P2（按方案编号，仅列本次复审新增的观察）

- **P1-F 顶部一行化**：未动，`--ink-shell-height` 仍 141px，研墨 / 织卷 / 落笔顶部仍 2–3 行。
- **P1-H 状态色与动效**：`animate-spin` 仍在 `ChapterReader` 加载态 / 保存按钮、`BookDetail` `write-next-primary` 等处；`BookStudy` 的已随 D 删除。`review-queue` 去绿底已随 E 顺手完成，可从 H 的清单里划掉。
- **P1-J 文案与 `inferGroundDone`**：U-C-8 文案已在 A 完成；`inferGroundDone` 仍是「采用 1 条即打勾」，J 只剩推断逻辑。
- **P1-K 问心记录归位**：「问心记录」仍在配置菜单。
- **P2-L 死代码**：`StageTools.tsx` 内第三份 `AuthoringRolesPanel`、`Sidebar.tsx` 等仍在，`SerialCockpit.tsx` 注释仍写「连载书房」；本次 `Sidebar.tsx` 顺手改了一处 `rounded-[10px]`，删除时无需保留。
- **P2-M 死文案**：`dash.subtitle` 现为「打开一本书进入本书」（无调用点、读起来别扭），随 M 删除即可，不必单修。
- **P2-P 短篇壳**：`ShortReader` 的「书房」返回按钮（`short-study-home`）与 `lib/short-study.ts` 注释「创作书房」仍违反词表「书房只作品牌语」。
- **旧书管线文案**：`activity-copy.ts`「落墨 · 第 N 章 起草中 / 完成 / 未完成」（运行 ▸ 实时动态、守护进程页）与 `serial-cockpit.ts`「请先落墨第 N 章」是旧管线事件与门禁文案，方案 A 的文件列表未包含；建议随 P1-J 或 P2-M 一并按「写下一章（旧管线）」口径替换，`activity-copy.test.ts` 同步。

### 3.3 与流程侧并行（沿用上轮结论，不变）

PR-G-lite 的 B-1 / B-2 / B-3 已在 #134 合入；F-7（织卷审查后台化）仍属流程侧，UI 轮不碰。

---

## 四、验证方式

1. **代码**：`ELECTRON_SKIP_BINARY_DOWNLOAD=1 pnpm install --frozen-lockfile` → `pnpm typecheck`（core build + studio client/server）通过；`pnpm --filter @actalk/inkos-studio exec vitest run` 上述 19 个文件 142 例全绿（4.7s）。未跑完整 `pnpm test`（本轮改动不触及 core / desktop / api 路由，#136 已在 CI 白名单里追加 `ink-copy-glossary / ink-ui-p0 / ink-home` 三个文件）。
2. **源码扫描**：`rg` 结果已在「二」逐条列出；`rounded-[10px]` 全 `src` 0 处、`name="export-format` 1 处、`<AuthoringRolesPanel` 2 处（1 处死代码）、禁用词 9 文件 0 处。
3. **不做**：未启动 Electron / Studio 引擎做实机；按方案「七」，1480×960 与 1100×720 双档、日夜 × 三种纸色、四阶段书 + 导入旧书的逐页验收由用户执行，检查项照方案「七·2」①–⑤。

---

## 附录 A · 为什么本轮没有随文做极小修复

任务允许对「一行级明确回归且直接误导用户」做极小修复。逐项核对后，**没有一处满足「回归 + 一行 + 误导」三条**：

| 候选 | 为什么不修 |
|---|---|
| `ChapterReader / TruthFiles` 加载窗口内闪现完整工具栏 | 不是回归（#136 前始终显示）；正确修法要区分「未知 / 四阶段 / 旧书 / 请求失败」四态，不是一行 |
| `settings.modelOverridesHint`「审稿」、`nav.deleteAskHint`「书稿」 | 纯用词，不改变任何操作含义；随 P1-A' 一起补测试更稳 |
| 书名按钮无 hover 态 | 是可发现性，不是误导；一行 CSS 但属视觉打磨（P1-D'） |
| `dash.subtitle`「打开一本书进入本书」 | 无调用点，用户看不到 |
